# Entire string will be fetched bbut only character at index 0
# is assigned to x so that x only gets a har value
x = input("Enter a character ")[0]

print(x,"is the input character")
print(type(x)) # type will be str only as there is not type char in python
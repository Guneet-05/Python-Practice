# In python, the input is always interpreted as a string
# So, to work with integers or any other datatype
#  we need to convert the type during taking the input

x = int(input("Enter the first number "))
y = int(input("Enter the second number "))

print("The sum is",x+y)
